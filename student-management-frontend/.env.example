# The Angular app uses src/environments/environment.ts for development and
# environment.prod.ts for production. Set API_URL in your deployment pipeline
# and generate that production file, or use a reverse proxy that serves the API at /api.
API_URL=https://your-api-domain.example
